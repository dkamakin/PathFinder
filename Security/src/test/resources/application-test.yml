spring:
  cloud:
    config:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    url: jdbc:h2:mem:foo;DB_CLOSE_ON_EXIT=FALSE
    password: ''
    username: sa
logging:
  level:
    root: debug