dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'PathFinder'

include("Core", "Searcher", "Security", "Configuration", "Security-Api", "Messaging", "Core-Web")